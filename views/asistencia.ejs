<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Asistencia</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-4">
    <h2 class="mb-4">Registro de Asistencia</h2>
<<<<<<< HEAD
    <a href="/IES_9-024/formacion-continua" class="btn btn-primary mb-3">volver a inscripciones</a>
    <a href="/IES_9-024/asistencia/lector" class="btn btn-primary mb-3">Abrir Lector QR</a>
    
    <form id="filtro-form" method="get" style="margin-bottom: 16px;">
      <label for="filtro-materia"><b>Filtrar por materia:</b></label>
      <select id="filtro-materia" name="materia" style="padding:6px; border-radius:6px;">
        <option value="todas" <%= materiaSeleccionada === 'todas' ? 'selected' : '' %>>Todas</option>
        <% if (materias && materias.length > 0) { %>
          <% materias.forEach(function(m) { %>
            <option value="<%= m.idmateria %>" <%= materiaSeleccionada == m.idmateria ? 'selected' : '' %>><%= m.materia %></option>
          <% }); %>
        <% } %>
      </select>
      <button type="submit" class="btn btn-primary btn-sm">Filtrar</button>
    </form>

=======
>>>>>>> c97512bca4c8d8719d72d67b95e517257b21a037
    <div class="table-responsive">
      <table class="table table-bordered table-striped align-middle">
        <thead class="table-dark">
          <tr>
            <th>N°</th>
            <th>ID Registro</th>
            <th>Fecha</th>
            <th>Hora</th>
            <th>Apellido</th>
            <th>Nombre</th>
            <th>Correo</th>
            <th>Teléfono</th>
            <th>Asistencia</th>
            <th>Inscripción</th>
            <th>A1</th>
            <th>AT</th>
            <th>AE</th>
            <th>Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% if (asistencias && asistencias.length > 0) { %>
            <% asistencias.forEach(function(a, i) { %>
              <tr>
                <td><%= i + 1 %></td>
                <td><%= a.idregistro %></td>
                <td><%= a.fecha %></td>
                <td><%= a.hora %></td>
                <td><%= a.apellido %></td>
                <td><%= a.nombre %></td>
                <td><%= a.correo %></td>
                <td><%= a.telefono %></td>
                <td><%= a.asistencia %></td>
                <td><%= a.inscripcion %></td>
                <td><%= a.A1 %></td>
                <td><%= a.AT %></td>
                <td><%= a.AE %></td>
                <td>
                  <!-- Botones de acción, por ejemplo: -->
                  <a href="/asistencia/editar/<%= a.idregistro %>" class="btn btn-warning btn-sm">Editar</a>
                  <form action="/asistencia/eliminar/<%= a.idregistro %>" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                  </form>
                </td>
              </tr>
            <% }); %>
          <% } else { %>
            <tr>
              <td colspan="14" class="text-center">No hay registros de asistencia.</td>
            </tr>
          <% } %>
        </tbody>
      </table>